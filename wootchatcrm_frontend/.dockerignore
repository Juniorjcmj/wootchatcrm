# Apenas o que NÃO precisa entrar no build context.
# Os arquivos de config (Dockerfile, nginx.conf, docker-entrypoint.sh,
# config.template.js) PRECISAM existir no contexto — o Dockerfile os
# move/copia pros locais corretos e remove do html/ depois.

logs
*.log
.git
.gitignore
.DS_Store
.claude
.vscode
.idea
node_modules
